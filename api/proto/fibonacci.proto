syntax = "proto3";

package fibonacci;

option go_package = "api/proto";

service fibonacci {
    rpc GetSequence(AddRequest) returns (AddResponse) {}
}

message AddRequest {
    int64 start = 1;
    int64 end = 2;
}

message AddResponse {
    map<int64, uint64> result = 1;
}
