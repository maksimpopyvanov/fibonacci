syntax = "proto3";

package fibonacci;

option go_package = "api/proto";

service fibonacci {
    rpc GetSequence(Request) returns (Response) {}
}

message Request {
    uint64 start = 1;
    uint64 end = 2;
}

message Response {
    map<uint64, string> result = 1;
}
