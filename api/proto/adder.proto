syntax = "proto3";

package api;

service fibonacci {
    rpc GetSequence(AddRequest) returns (AddResponse) {}
}

message AddRequest {
    int start = 1;
    int end = 2;
}

message AddResponse {
    map[int]int result = 1;
}
